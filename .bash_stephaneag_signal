## - Audio & Signal analysis -
# open baudline with sound from soundcard as input
alias stag_baudline='parec --format=s16le --channels=1 --latency-msec=5 | baudline -stdin'
# open xoscope with sound from sound card as input
alias stag_xoscope='padsp xoscope'
